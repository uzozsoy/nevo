# Optional dependencies for ONNX export, .nevo bundles, and offline benchmarks.
#
# Requires torch and torchaudio to be installed separately.

-r requirements.txt

onnx==1.21.0
onnxruntime==1.25.1
psutil==7.2.2
soundfile==0.13.1
